# Conventions
<!-- human-owned: agents read this, never edit it. -->

Project decisions that code inspection can't reveal. Laravel Boost already
gives agents framework conventions and package versions — record here only
what is specific to THIS team.

## Architecture decisions
<!-- e.g. "Actions pattern for business logic, controllers stay thin",
     "No repositories — Eloquent directly", "Feature folders under app/Domain". -->

## Naming & structure
<!-- e.g. route naming scheme, API response envelope, event naming. -->

## Testing policy
<!-- e.g. "Feature tests over unit tests", "Factories for all models",
     "No mocking Eloquent". -->

## Forbidden
<!-- Things agents must never do here: e.g. "no new composer deps without
     human approval", "never touch legacy/ directory". -->
